apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: auction
  labels:
    monitoring: prometheus
spec:
  type: LoadBalancer
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8080
