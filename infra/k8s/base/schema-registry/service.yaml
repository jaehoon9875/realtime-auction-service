apiVersion: v1
kind: Service
metadata:
  name: schema-registry-service
  namespace: auction
spec:
  selector:
    app: schema-registry
  ports:
    - name: http
      port: 8085
      targetPort: 8085
