apiVersion: v1
kind: ConfigMap
metadata:
  name: debezium-connector-configmap
  namespace: auction
data:
  # Cloud SQL Auth Proxy 사이드카가 각 DB 를 로컬 포트로 프록시한다.
  # auction-db → localhost:5432 (cloud-sql-proxy-auction)
  # bid-db    → localhost:5433 (cloud-sql-proxy-bid)
  AUCTION_POSTGRES_HOST: localhost
  AUCTION_POSTGRES_PORT: "5432"
  AUCTION_POSTGRES_DB: auction_db
  BID_POSTGRES_HOST: localhost
  BID_POSTGRES_PORT: "5433"
  BID_POSTGRES_DB: bid_db
