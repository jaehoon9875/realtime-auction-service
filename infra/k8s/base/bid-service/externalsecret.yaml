apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: bid-service-externalsecret
  namespace: auction
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: bid-service-secret
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: bid-db-password
    - secretKey: INTERNAL_REQUEST_SECRET
      remoteRef:
        key: internal-request-secret
